<Window x:Class="DocToPdfTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DocToPdfTool - 文档转PDF工具"
        Width="620"
        Height="300"
        MinWidth="520"
        MinHeight="260"
        WindowStartupLocation="CenterScreen">
    <Grid Background="White" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="文档转 PDF 工具，支持 Word、Excel、PPT、TXT、RTF、HTML 等格式"
                       Foreground="#666666"/>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="转换引擎:" VerticalAlignment="Center" Foreground="#888888" FontSize="12"/>
                <RadioButton x:Name="RadioWps" GroupName="EngineGroup" Content="WPS Office" IsChecked="True"
                             Margin="8,0,16,0" VerticalAlignment="Center"/>
                <RadioButton x:Name="RadioOffice" GroupName="EngineGroup" Content="Microsoft Office"
                             VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <!-- 源文件 -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="源文件:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="TxtSourceFile" IsReadOnly="True"
                         VerticalAlignment="Center" />
                <Button Grid.Column="2" Content="浏览" Padding="12,4" Margin="8,0,0,0"
                        Click="BtnBrowseSource_Click"/>
            </Grid>
        </Border>

        <!-- 输出目录 -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,12,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="输出目录:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="TxtOutputDir" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="浏览" Padding="12,4" Margin="8,0,0,0"
                        Click="BtnBrowseOutput_Click"/>
            </Grid>
        </Border>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="BtnConvert" Content="执 行 转 换" Padding="24,8" Width="160"
                    Click="BtnConvert_Click"/>
            <Button Content="退 出" Padding="24,8" Width="160" Margin="16,0,0,0"
                    Click="BtnExit_Click"/>
        </StackPanel>

        <!-- 状态提示 -->
        <TextBlock Grid.Row="4" x:Name="TxtStatus"
                   Foreground="#999999" FontSize="12"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Margin="0,8,0,0" Text=""/>

        <ToggleButton x:Name="BtnTopmost" Content="📌 置顶"
                      HorizontalAlignment="Right" VerticalAlignment="Top"
                      Margin="0,-8,-12,0"
                      Click="BtnTopmost_Click" Style="{StaticResource TopmostToggleButton}"/>
    </Grid>
</Window>